import { PageSlideDirective } from './page-slide.directive';

describe('PageSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new PageSlideDirective();
    expect(directive).toBeTruthy();
  });
});
