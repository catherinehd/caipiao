import { ElemHeightDirective } from './elem-height.directive';

describe('ElemHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ElemHeightDirective();
    expect(directive).toBeTruthy();
  });
});
