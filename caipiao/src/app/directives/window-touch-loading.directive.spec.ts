import { WindowTouchLoadingDirective } from './window-touch-loading.directive';

describe('WindowTouchLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowTouchLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
