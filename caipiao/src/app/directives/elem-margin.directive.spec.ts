import { ElemMarginDirective } from './elem-margin.directive';

describe('ElemMarginDirective', () => {
  it('should create an instance', () => {
    const directive = new ElemMarginDirective();
    expect(directive).toBeTruthy();
  });
});
